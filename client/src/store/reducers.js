const initialState = {
  user: [],
  games: [],
  questions: [],
  isLoading: false,
  isAuth: false,
  error: null,
};

export const reducers = (state = initialState, action) => {
  switch (action) {
    default:
      return state;
  }
};
